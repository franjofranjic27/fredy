commit-msg:
  commands:
    validate-message:
      run: bash scripts/validate-commit-msg.sh {1}

pre-commit:
  parallel: true
  commands:
    typecheck-agent-core:
      run: cd packages/agent-core && pnpm exec tsc --noEmit
    lint-agent-core:
      run: cd packages/agent-core && pnpm lint
    typecheck-agent:
      run: cd services/agent && pnpm exec tsc --noEmit
    lint-agent:
      run: cd services/agent && pnpm lint
    typecheck-jira-agent:
      run: cd services/jira-agent && pnpm exec tsc --noEmit
    lint-jira-agent:
      run: cd services/jira-agent && pnpm lint
    lint-confluence-importer:
      run: cd services/confluence-importer && uv run ruff check . && uv run ruff format --check .
    lint-eval:
      run: cd services/eval && uv run ruff check . && uv run ruff format --check .
    test-agent-core:
      run: cd packages/agent-core && pnpm test
    test-agent:
      run: cd services/agent && pnpm test
    test-jira-agent:
      run: cd services/jira-agent && pnpm test
    test-confluence-importer:
      run: cd services/confluence-importer && uv run pytest
    test-eval:
      run: cd services/eval && uv run pytest
    check-secrets:
      run: bash scripts/check-secrets.sh
